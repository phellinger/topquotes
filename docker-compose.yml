version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Use bind mount to sync with local quotes.json file
      - ./quotes.json:/app/quotes.json
    restart: unless-stopped

